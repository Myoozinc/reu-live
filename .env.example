# Copia a .env para uso local con `vercel dev`, o configura en Vercel:
# Project Settings -> Environment Variables.
#
# Con GROQ_API_KEY configurada, la app usa:
# - IA real (Llama 3.3 70B) para el copiloto en /api/chat
# - Whisper real (Groq) para transcribir el audio grabado en /api/transcribe
#
# OPENROUTER_API_KEY es opcional, solo se usa como respaldo del chat si Groq falla.

GROQ_API_KEY=gsk_xxxxxxxxxxxxxxxxxxxxxxxx
OPENROUTER_API_KEY=sk-or-xxxxxxxxxxxxxxxxxxxxxxxx
